import styled from "styled-components";

const PayResult = () => {
  return <PayResultBox>PayFilter</PayResultBox>;
};

export default PayResult;

//styled-components
const PayResultBox = styled.div`
  margin-top: 1rem;
  background-color: white;
  display: flex;
  flex-direction: column;
`;
